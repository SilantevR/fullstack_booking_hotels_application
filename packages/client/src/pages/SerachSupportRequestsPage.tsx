import React from "react";
import { SearchSupportRequests } from "../features/supportRequest/components/SearchSupportRequests";

export const SearchSupportRequestPage: React.FC = () => {
  return <SearchSupportRequests />;
};
